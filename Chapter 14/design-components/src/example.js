function Layers() {
    // Vertically layer children on top of each other
}

function Inline() {
    // Horizontally layer children in row
}

function Split() {
    // Split the width of the parent between two children
}

function Cover() {
    // Vertically center children
}